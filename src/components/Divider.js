import React from 'react'

const Divider = ({width, marginBottom, marginLeft, marginTop}) => {
  return (
    <div className='divider' style={{width: width, marginBottom: marginBottom, marginLeft: marginLeft, marginTop:marginTop}}>
    </div>
  )
}

export default Divider